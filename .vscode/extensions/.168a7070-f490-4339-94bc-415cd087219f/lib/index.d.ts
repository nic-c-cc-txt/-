import { ExtensionContext } from "vscode";
/**
 * Activates the extension.
 *
 * @param context
 * The extension-context.
 */
export declare let activate: (context: ExtensionContext) => Promise<void>;
/**
 * Deactivates the extension.
 */
export declare let deactivate: () => void;
