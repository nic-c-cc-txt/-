import { IInterception } from "./IInterception";
/**
 * Represents a collection of interceptions.
 */
export declare class InterceptionCollection<T extends object> extends Map<keyof T, IInterception<T, keyof T>> {
    /**
     * Gets an interception of the collection.
     *
     * @param key
     * The key whose value to get.
     *
     * @returns
     * The interception with the specified key.
     */
    get<TKey extends keyof T>(key: TKey): IInterception<T, TKey>;
    /**
     * Sets an interception of the collection.
     *
     * @param key
     * The key whose value to set.
     *
     * @param value
     * The value to set.
     *
     * @returns
     * This collection.
     */
    set<TKey extends keyof T>(key: TKey, value: IInterception<T, TKey>): this;
}
