import { Linter } from "eslint";
import { DiagnosticCategory, DiagnosticMessageChain, SourceFile } from "typescript/lib/tsserverlibrary";
import { Range } from "vscode-languageserver";
import { Plugin } from "../Plugin";
import { Diagnostic } from "./Diagnostic";
/**
 * Represents a diagnostic which provides information about an `eslint` failure.
 */
export declare class ESLintDiagnostic extends Diagnostic {
    /**
     * The lint failure.
     */
    private lintMessage;
    /**
     * Initializes a new instance of the `ESLintDiagnostic`.
     *
     * @param plugin
     * The plugin of the diagnostic.
     *
     * @param file
     * The file of the diagnostic.
     *
     * @param lintMessage
     * The lint failure.
     */
    constructor(plugin: Plugin, file: SourceFile, lintMessage: Linter.LintMessage);
    /**
     * Gets the lint failure.
     */
    get LintMessage(): Linter.LintMessage;
    /**
     * @inheritdoc
     */
    get Category(): DiagnosticCategory;
    /**
     * @inheritdoc
     */
    get Range(): Range;
    /**
     * @inheritdoc
     */
    get Message(): string | DiagnosticMessageChain;
}
