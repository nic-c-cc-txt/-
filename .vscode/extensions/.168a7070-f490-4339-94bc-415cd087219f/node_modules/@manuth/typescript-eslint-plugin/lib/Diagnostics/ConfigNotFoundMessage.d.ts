import { DiagnosticCategory, SourceFile } from "typescript/lib/tsserverlibrary";
import { Plugin } from "../Plugin";
import { Diagnostic } from "./Diagnostic";
/**
 * Represents a message for a missing configuration.
 */
export declare class ConfigNotFoundMessage extends Diagnostic {
    /**
     * The actual exception.
     */
    private exception;
    /**
     * Initializes a new instance of the `ConfigNotFoundMessage` class.
     *
     * @param plugin
     * The plugin of the diagnostic.
     *
     * @param file
     * The file of the diagnostic.
     *
     * @param exception
     * The actual exception.
     *
     * @param category
     * The category of the message.
     */
    constructor(plugin: Plugin, file: SourceFile, exception: Error, category?: DiagnosticCategory);
    /**
     * @inheritdoc
     */
    get Message(): string;
}
